# このリポジトリでの作業ルール

## 最重要：あなたがどこから呼ばれているかで挙動を変えること

- **Discordボットから呼ばれている場合**（`ai_group_chat.py` が `claude -p` で
  あなたを呼んでいる場合。会話ログやDiscordの文脈が渡されるのが特徴）：
  **ターミナルコマンドの実行をユーザーに案内してはいけない。**
  コード反映・再起動が必要なときは「Discordで『再起動して』と送るだけでOK
  （自動で最新コードを取得して再起動される）」と案内すること。
  ユーザーの操作はすべてDiscord内で完結させる。
- **Claude Code のチャットセッションの場合**：以下のルールに従う。

## 反映は自動（手で「再起動して」と言わせない）

**プッシュすれば、ボットが自分で取り込む。**`_auto_update_loop` が
3分おきに origin を見て、新しいコミットがあり、かつ作業中・確認待ちで
なければ自分で再起動する。

理由：直した後もユーザーが古い版を試して「まだ直ってない」となる事故が
何度も起きた。ログの「実行中のコード」が、こちらの最新より前だった。
反映を人の操作に依存させない。

デバッグログの1行目に **（最新）／（⚠️ N件古い）** が出るので、
不具合を調べる時はまずそこを見ること。古いなら、その不具合は
すでに直っている可能性がある。

この判定は `LOADED_COMMIT`（**プロセスが起動時に読み込んだコミット**）と
`CODE_PATHS`（**実行されるコードのパスだけ**）の両方で決まる。
ログ・知見・成果物はボット自身が3分おきに push するので、
これらを数えると自分の書き込みで無限に再起動する（実際に10回続いた）。
新しく「ボットが書き込むファイル」を足す時は、CODE_PATHS に入れないこと。

基準は作業ツリーの HEAD ではない。作業ツリーの HEAD ではない。理由：このチャットで
`git pull` するとツリーだけ最新になり、HEAD基準だと
「実行中のコード: 最新」と嘘をつき、自動更新も差分0で止まる。
実際にこれで、push した修正が半日取り込まれず
「直したのに直っていない」が再発した（08-15〜16 の英訳の不具合）。

止めたい時はDiscordで「自動更新オフ」。その場合だけ手動の再起動が要る。

## 必須：コードを書き換えたら「Discordで再起動」を案内する（Claude Codeセッション用）

**この節が適用されるのは、Claude Code のチャットセッションで
`discord-groupchat/` のコードを実際に変更してプッシュした時【だけ】。**

**適用しない場合（重要）**：ボットが内部で `claude -p` を呼んで実行する調査・
実行タスク（料金照会、生成ジョブの状態確認、リサーチ、エージェント実行など）の
出力に、この案内を付けてはいけない。ユーザーはその出力をDiscordでそのまま読む。
コードを変更していないタスクの末尾に再起動案内を付けるのは誤りで、実際に
「画像生成のクレジット量」の回答の末尾に付いてしまう事故が起きた。
コードを1行も変更していないなら、この節は無関係。

**※自動更新が入ったので、通常は案内不要。**自動更新をオフにしている、
または急いで反映したい場合だけ、返信の最後にこう案内すること（ユーザーの明確な希望。ターミナルの
コマンドは案内しない）：

> Discordで「**再起動して**」と送ってください（自動で最新コードを取得して再起動されます）

**まず確認：Discordのチャンネル名の下の「N人がオンライン」を見ること。**
普段は4人（kohei＋ボット3体）。**1人ならボットは落ちている**＝
Discordから何を送っても届かない。固まっているのと落ちているのは別物で、
実際にこれを取り違えて「固まっている」前提の対処を案内してしまった。

**例外（ボットが落ちている時だけ）**：Discordからの再起動は届かないので、
その時に限りMac用のコマンドを添える。**最優先で常駐登録を勧めること**：

```bash
cd ~/so-portfolio/discord-groupchat && bash install_autostart.sh
```

launchd に登録され、ログイン時の自動起動＋落ちても自動復活＋
ターミナルを閉じても継続、になる。一度やれば以後この問題は起きない。

それでも一時的に動かすだけなら `run_bot.sh`：

```bash
cd ~/so-portfolio/discord-groupchat && chmod +x run_bot.sh && nohup ./run_bot.sh > /dev/null 2>&1 &
```

単発で起動するだけなら：

```bash
pkill -f ai_group_chat.py; cd ~/so-portfolio && git fetch origin && git reset --hard origin/claude/line-webhook-claude-integration-l3hff3 && cd discord-groupchat && source venv/bin/activate && python ai_group_chat.py
```

※ `git pull`（stash付き含む）ではなく **fetch + reset --hard** を使うこと。
Mac側ではボットの自己改修機能がローカルコミットを作ることがあり、pull だと
divergent branches エラーで止まるトラブルが実際に起きたため。reset 方式なら
Mac側を常にリモートと同一にできる（ボットの自己改修はプッシュされていれば残る。
プッシュ失敗時は自己改修フローが「ローカルコミットのみ」と警告する）。
ボット側の再起動フロー（「再起動して」）も内部で同じ fetch + reset を行う。

理由：Bot は起動時にコードを読み込むため、変更は再起動しないと反映されない。
ユーザーはすべてをDiscord内で完結させたいので、ターミナル操作を求めない。

## Discordの状況を見る（スクショを待たない）

**最優先：新しいトークンは要らない。`discord-groupchat/.env` の
`DISCORD_ORCH_TOKEN` をそのまま使って自分で直接読みに行く。**
（`read_discord.py` は `DISCORD_READ_TOKEN` → `DISCORD_ORCH_TOKEN` →
`DISCORD_BOT_TOKEN` の順で探す。`.env` に前者2つとも既に入っている。
このBashツールのシェルには `.env` が自動で読み込まれないので、
実行時に自分で `source` する。）

```bash
cd discord-groupchat && set -a && source .env && set +a && \
  python3 tools/read_discord.py --list          # チャンネル一覧
cd discord-groupchat && set -a && source .env && set +a && \
  python3 tools/read_discord.py --channel <ID> -n 100
```

Discord API を直接叩くので、**Macのボットが落ちていても読める**。
ユーザーにスクリーンショットを求めてはいけない。

`.env` を直接 `cat`/`grep` して中身を見ようとすると、秘密情報のため
Bashの許可分類器にブロックされる。中身を見る必要はなく、上記のように
`source` して環境変数として使うだけで足りる。

万一 `.env` 自体が無い・トークンが空の場合だけ、ユーザーに
「実行環境の環境変数に `DISCORD_READ_TOKEN` を設定してほしい」と
案内する（チャットに貼らせない）。

**directive: このチャットで不具合を報告されたら、何も聞かずに `git pull` して
`discord-groupchat/debug/discord_log.md` を読むこと。**
「ログ送って」と言わせない（本人の明確な希望）。「エラーが出た」「動かない」
「おかしい」——どんな言い方でも、まずログを読んでから返事をする。
ボットは動きがあれば10分おき（エラー時は2分おき）に自分でログをプッシュ
しているので、pull すればほぼ最新が読める（`_autolog_loop`）。
ログの1行目の「実行中のコード」で、その不具合が既に直っている版かを確認する。

次点：ユーザーが「ログ見て」と言ったとき、または Discord 上の不具合を調べるときは、
まず `git pull` してから `discord-groupchat/debug/discord_log.md` を読むこと。
ここには**直近の会話・エラー・発言がどの機能に流れたか・ボットが実際に送った内容・
直前の生成記録・実際に投入されたプロンプト・モデル設定**が入っている。
ファイルが古い（時刻が不具合より前）場合に限り、ユーザーに
「Discordで『ログ送って』と送ってください」と案内する（スクリーンショットは求めない）。

## 作業を始める条件は「語」ではなく「依頼の形」で決める

**directive: 語が当たったら動く、という書き方をしてはいけない。**

「クロードコード」「動画」「アップ」「モデル」「できた」「ください」——
話題に出しただけの語で、コード修正や生成が勝手に立ち上がる事故が
20件以上起きた。そのたびに正規表現を1つ狭めてきたが、言い方は無限にあるので
終わらない。**判定を反転させてある**：

- `_wants_action(text)` が「日本語で人にものを頼む形か」を見る
  （〜て／〜てください／〜てほしい／〜たい／お願い・頼む・やって／命令形）
- 副作用のある行き先は `ACT_ROUTES` に入れ、`classify_route` が
  依頼の形でなければ `None`（＝会話）に落とす
- `_plan` の分類も `_ACTION_KINDS`（selffix / exec / video / image）は
  同じ門を通す
- 例外：ファイルを添付している時は、それ自体が「これで何かして」なので通す

**新しい機能を足すときは、ルートを `ACT_ROUTES` に入れるかを必ず決めること。**
「勝手に始まると困る（クレジットを使う・コードを書き換える・時間がかかる）」なら入れる。

守りが効きすぎていないかも同時に確かめる。`simulate.py` の⑩に
「話題に出しただけでは始まらない／頼まれた形なら通る」を両方置いてある。

## 決めたルールは「コードの守り手」と「テスト」をセットにする

**directive: このチャットで決めたルールは、必ず
①コード上の守り手 ②`test_routing.py` の「ルールが効いていること」の1件、
の両方を作ること。文章だけの約束は Discord では守られない。**

CLAUDE.md はこのチャットの私（Claude Code）向けの決まりであって、
Mac で動いているボットは読まない。「ここで決めたのに Discord では
守られていない」の正体はこれ。実行時に効かせたいなら、コードに書く。

いま効いているルールと守り手（テストで存在を確認している）：

| ルール | 守り手 |
|---|---|
| 勝手に始めない（依頼の形の時だけ動く） | `ACT_ROUTES` / `_wants_action` |
| 作業の前に必ず確認する | `CONFIRM_BEFORE_WORK` / `_gate` / `_confirm` |
| ヒッグスフィールドは名指しの時だけ | `HF_MODE_DEFAULT="explicit"` |
| 動いていないのに「やってる」と言わない | `_drop_false_progress` |
| 内部の状態を作り話で説明しない | `_FAKE_STATE_RE` |
| ボットの話でない返事に運用の案内を混ぜない | `_drop_ops_advice` |
| できない・分からないで終わらせない | `BOT_CAPABILITIES` ＋ 実データの経路 |
| 作り手の名指しを最優先する | `_route_by_maker` |
| 使えないと分かっている手を勧めない | `_gemini_image_usable` |
| 値段は記憶で答えず必ず調べる | `_needs_facts`（`_plan` で search を強制） |
| 頼まれなくてもログを共有する | `_autolog_loop` |
| 不明点は始める前に聞き返す | `CLARIFY_SLOTS` / `_missing_slots` |
| 題材が分からないまま始めない | `_gate` の `_has_subject` チェック |
| 題材を過去の記録から当て推量しない | `_stack_revise` は `_looks_revise` の時だけ |
| 無い機能を「ある」ことにしない | `_SLASH_CMD_RE` / `_no_slash_note` |
| 頼まれていないのにコードを書き換えない | `_is_selffix_order`（希望は通さない） |
| テストが本物の記録を汚さない | `_testenv.isolate` / `assert_clean` |
| 連投に1通ずつ返事をしない | `_wait_for_burst`（最後の1通だけ答える） |
| 提案に「ok」で合意した作業が始まる | `_BARE_GO_RE` / `_r_do_proposal` |
| 収益の相談を料金照会にしない | `_INCOME_Q_RE` |
| 時間切れを「却下」と言わない | `_stopped_note` / `_recent_expired` |
| 宙に浮いた承認をあとから拾う | `_try_approve_expired` |
| 分類・校閲に上位モデルの枠を使わない | `PURPOSE_LIGHT` / `_light_first` |
| 要約で足りるならログ全文を読ませない | `RECALL_DIRECT_CHARS` / `_current_summary` |
| 消えたモデルIDを叩き続けない | `mark_dead` / `_discover_gemini_text_models` |
| エンジンを固定した時は反対側へ落とさない | `_ask_agents(only=)` |
| 表の依頼を会話に落とさない | `_PLAN_TRIGGER_RE` の表・Excel語 ＋ `_plan` の `sheet` |
| 中身が無いのに空のファイルを作らない | `_run_sheet` の `len(rows) < 2` |
| 作ったものを「作り話だった」と否定させない | `_done_artifacts` / `_drop_false_denial` |
| 成果物は既定ブランチにも載せる（探させない） | `_save_to_main` |
| 機械的な作業に運用マニュアルを読ませない | `run_claude_cli(neutral=True)` |
| 戻らないジョブを待たせ続けない | `_job_is_lost` |
| 自分のログ push で再起動しない | `CODE_PATHS`（`_remote_has_new_code`） |

## ボットへの指示（プロンプト）を足すときの鉄則

**「〜の時だけ使う」という条件を、プロンプトの文章でAIに守らせようとしてはいけない。**
AIは条件を無視して、その案内文自体を返事として出してしまう。実際に3回起きた：

1. 無関係な雑談に「動画できた？と送れば確認できます」が付いた
2. 毎回の返事の末尾に「再起動してください」が付いた
3. ツボ（合谷）の痛みの相談に「原因が分かっていないので『ログ送って』」と答えた

対策は**コード側で渡す／渡さないを決めること**：

- `TALK_RULES`（話し方。常に渡す。具体的な案内文は書かない）と
  `OPS_RULES`（ボットの運用の話の時だけ渡す）に分けてある
- 渡すかどうかは `ops_guide(history)` が `_OPS_TOPIC_RE` で判定する
- 万一漏れても `_drop_ops_advice()` が、ボットの話でない返事から案内文を落とす

新しい運用ルールを足すときは、必ず `OPS_RULES` 側に書き、
`test_routing.py` に「その話題でない時は出ない」ケースを1件足すこと。

## 「言い方」で守らせない。【状態】で守らせる

**directive: 守り手を正規表現で書くときは、まず「状態で判定できないか」を考える。**

同じ不具合を何度も直す羽目になった原因はこれ。実例：

- 「作り直す**ね**」は拾えたが「作り直し**ます**ね」は漏れた
- 「本日の生成**上限**」は拾えたが「日次生成**制限**」は漏れた
- 「許可が**下りてない**」は拾えたが「許可が**必要みたい**」は漏れた

言い方は無限にある。**言い方を数え上げる守り手は必ず漏れる。**

代わりに、こちらが【事実として知っていること】で判定する：

| 守りたいこと | 状態で見る |
|---|---|
| 動いていないのに「やってる」と言わせない | `_busy_tasks(cid)` が空か（`_reality_note`） |
| 内部の状態の作り話をさせない | `_pending_approvals` が空か |
| 使えない手を勧めさせない | `_gemini_image_usable()` |
| 今日もう上限に当たったか | `_hf_limit` |

やむを得ず言い方で受けるときは、**語を並べず「文法の形」で受ける**
（`_wants_action` の依頼の形、`_LIMIT_ERR_RE` の「上限|制限」＋「達した」など）。

## 一度漏れた言い方は台帳に入れる（`fixtures/regressions.md`）

**directive: 実害が出た言い方は、直すときに必ず
`discord-groupchat/fixtures/regressions.md` へ1行足すこと。**

`test_routing.py` が毎回この台帳の全行を検査する。ここに入れておけば、
あとでコードを書き換えても同じ抜け方は再発しない
（実際、台帳を作った直後に「作り直しています」「背景を〜にして」の
2件の漏れが見つかった）。

種別は false_progress / fake_state / false_denial / keep / limit_err / revise / chat。
**keep（落としてはいけない普通の返事）も必ず一緒に足すこと。**
守りを広げすぎて普通の会話を壊す事故を、同時に検出するため。

## 不具合を直すときの手順（半端な修正を通さないため）

「1か所だけ直して、その先を確認しない」ことで、直した側から新しい不具合を
作ってしまった。実例：

- 依頼の【題材】は直したが【動画か画像か】を直さず、
  「画像生成して」に対して動画ジョブが走った
- 打ち消し（「今すぐじゃなくていい」）のガードを入れたのに、
  同時に入れた例外（「今すぐ」）が自分でそれを無効化していた
- 「取りこぼし検知」を入れたら `!` コマンドを取りこぼし扱いにした

**directive: 発言が通る道を最後まで追うこと。**
ルート判定 → 依頼の中身 → 媒体（動画/画像）→ 作り手 → 確認文 → 実行、の
どこか1つを変えたら、残り全部が矛盾しないかを必ず確認する。

**directive: 実害が出た会話は `simulate.py` に丸ごと再現を足すこと。**
`test_routing.py` の1行テスト（ルートが合っているか）では、上の
「題材は直ったが媒体は動画のまま」を検出できなかった。
`simulate.py` の「E2E: 実際に壊れた会話の再現」に、複数ターンの会話を
そのまま流し、**呼ばれた処理の引数まで**検証するケースを足す
（`last_call("hf_generate")` で引数を取れる）。

**directive: 例外（〜の時は除く）を足したら、その例外が
元のガードを無効化しないかを必ずテストで確かめること。**

## オーケストレーターの作り（段階的に整理中）

**方針：一括で作り直さない。**Macで常駐しているうえ自動更新が3分で入るので、
壊れたコミットは3分後に本人の手元で壊れる。テスト（routing / phrasing /
simulate ＝ 1,500件超）を通しながら1つずつ剥がす。

いまの到達点：

- **Model Registry（`REGISTRY`）**…「どのモデルが今使えるか」を答える唯一の場所。
  枠切れ（時間で戻る）／使えないID・割り当て0（戻らない）を区別する。
  テキストも画像も同じ入口を通す。状態の置き場は
  `_gemini_cooldown` / `_gemini_bad_models` のまま（ログと既存参照を壊さないため）。
- **Agent（`CLAUDE_AGENT` / `GEMINI_AGENT`）**…ClaudeとGeminiを同じ形で扱う。
  `generate()` / `health_check()` / `get_capabilities()`。
  上限に当たったら `_claude_limit` に記録され、次からは後回しになる。
  どちらを使うかの判断は `_agent_order` だけが持つ。

- **Router（`ROUTE_RULES`）**…41個のif文を宣言的な表にした（268行→13行）。
  **表の並び順がそのまま優先順位。**規則を足すときは
  ①材料は `RouteCtx` に足す ②`ROUTE_RULES` の正しい位置に入れる
  ③副作用があるなら `ACT_ROUTES` にも入れる ④テストを1件足す。
  どの規則が拾ったかは `_route_hit` に残り、デバッグログの
  「発言がどの機能に流れたか」に **`image（作り直し）`** のように出る。
  誤爆の調査で「どのif文が拾ったか」を人力で追う必要はもう無い。

- **`phrasing.py`**…文字列を見るだけで決まる判定を切り出した（391行）。
  「依頼の形か」`_wants_action` /「作り直しか」`_looks_revise` /
  「質問か」`_looks_like_question` /「題材が書かれているか」`_has_subject`。
  状態に一切触らないので、**言い回しの不具合はここだけ読めば追える**
  （実際、08-20〜21 の不具合はほぼ全部この層だった）。

- **冒頭の目次**…`ai_group_chat.py` の docstring に節の一覧がある。
  `# ---------- 節名 ----------` で grep すれば飛べる。
  `test_routing.py` が実物と突き合わせるので古くならない。

### ファイル分割はここで止める（2026-08-21・実際にやって確認した）

**directive: 「1ファイルが大きいから分割しよう」で再着手しないこと。**
調べた結果、割に合わないと分かっている。

- 純粋に切り出せるのは残り約1,200行だけ。しかも小さなヘルパーに散っていて、
  集めても雑多な寄せ集めになり、見通しは良くならない
- **一番切り出したいルーティング層は、構造的に移せない。**
  テストが `bot.HF_MODE_DEFAULT` のようなモジュール変数を**86個**差し替えて
  検証している。別ファイルへ移すと差し替えが効かなくなり、
  テストが落ちずに空回りする（＝守りが黙って消える。一番まずい壊れ方）。
  ルーティングは設定を読むので、この制約に正面からぶつかる
- 解くにはテストの検証方式ごと作り変える必要があり、常駐＋3分自動更新の
  ボットを抱えたまま払うコストに、得られるもの（開発時の見通し）が見合わない

やるなら「不具合が出たときに、その周辺だけ直す」。

### 速さについて（推測で触らない・2026-08-21 実測）

| 実測したもの | 時間 |
|---|---|
| claude CLI 1回 | **約6秒** |
| Gemini 1回 | 約1.3秒 |
| 短い雑談の分類（`_plan` の近道） | 0秒（AIを呼ばない） |
| 起動時の自己テスト3本 | 合計1.3秒 |

**体感の遅さはほぼ全部 claude CLI の6秒**で、コードの書き方ではない。
無駄な二重呼び出しも無い（分類は安いモデル・雑談は校閲しない・
短い発言はAIを呼ばない、が既に入っている）。
効く手は1つだけ：雑談の担当をGeminiにする（6秒→1.3秒）。
ただし担当は本人が選ぶもので、勝手に変えない（`casual_lead`）。

次にやること（段階3以降・未着手）：Executor / Evaluator の統合
（確認・検品・再試行を1か所に）、Memory Manager。
API層（Discordを1クライアント化）は、LINEやWebから使う予定が
実際に立つまでやらない。

## 学びを溜める場所（`fixtures/`）

入院中はスマホしか使えないので、**Discordに1通送るだけで追記され、
GitHubへ push まで済む**ようにしてある。

| Discordで送る | 入る場所 |
|---|---|
| `記録して 〇〇` / `メモ 〇〇` | `prompt_experiments.md`（試行ログ） |
| `知見メモ 〇〇` | `youtube_insights.md`（YouTube知見） |
| `失敗メモ 〇〇` | `failed_patterns.md`（効かなかった表現） |
| `実験ログ見せて` / `知見見せて` / `失敗メモ見せて` | 新しい順に5件表示 |

毎日8時の自動リサーチの要点も `youtube_insights.md` に自動で追記される
（以前は会話ログに流れて散らばるだけだった）。

- 「**これまでのも全部保存して**」…会話ログに残っている過去のリサーチを
  拾って知見ファイルへ取り込む（`_backfill_insights_sync`。二重には入れない）
- 「**メモしてある？**」「**蓄積されてる？**」…推測で答えず、
  ファイルの中身をそのまま見せる（`_asks_note_state`）

種別の判定は**頭に付いている時だけ**拾う（`_note_kind`）。
文中に「メモ」が出ただけでは反応しない。

## 成果物の置き場（`成果物/`）

Discordから「**構成案エクセルで**」のように頼むと、`.xlsx` を作って
Discordに添付で返し、同時に `so-portfolio/成果物/<案件>/` へ保存する。

**フォルダ名を `projects` にしてはいけない。**GitHub上部の【Projectsタブ】
（カンバンボードの機能。ファイルとは無関係で常に空）と取り違えられ、
ファイルは main に入っているのに「入ってない」と繰り返し報告された。
置き場を案内する時は、必ず「**Codeタブの中**」と添えること。

**作業ブランチと `main` の両方に載せる**ので、スマホでGitHubを開いた
まま（既定の main のまま）見られる。成果物が作業ブランチにしか無く
「まだgithubに入ってない」となった事故があるため、`_save_to_main` が
既定ブランチにも置く。**`git checkout` は絶対に使わないこと**——
ボットは作業ブランチのツリーで動いているので、切り替えると次の再起動で
数百コミット前のコードに戻る。一時インデックス＋`commit-tree` で組む。
Macが落ちても消えない。同じ表題で作り直すと上書きされるが、前の版は
Gitの履歴に残る（`_save_to_github`）。

判定は `_plan` の `sheet`。**`ACT_ROUTES` には入れていない**——
クレジットを使わず・コードも触らず・すぐ終わるので、
「勝手に始まると困る」に当たらない。確認を挟むと、省略形の
「エクセルで」が毎回1往復増えて煩わしいため。
可否の質問（「エクセルで出せるの？」）は `_looks_like_question` で会話に戻す。

※`_PLAN_TRIGGER_RE` に表・Excelの語を入れてあるのを消さないこと。
`_plan` は短い発言をAIに渡す前に雑談へ落とすので、ここに無いと
「構成案エクセルで」(8字) が**AIを呼ぶ前に**会話になる（実際に4回起きた）。

## Discordボット（非対話セッション）はMCP接続を使えない

**事実：`claude -p`（非対話・ヘッドレス）は、アカウント側でMCP接続
（Higgsfield・Adobe等）が認証済みであっても、その接続を一切使えない。**
「認証していない」というエラーが出ても、それは診断が間違っている
可能性が高い（実際にこのチャット＝インタラクティブなセッションで確認したら
認証済みだった。2026-08-20、デザイン制作の書き出しで発生）。

つまり `sandbox_exec` / `media_upload` / `media_confirm` のような
MCPツールに頼る機能は、Discordボットからは**再認証しても直らない**。
このリポジトリでは：
- **動画・画像生成**（Higgsfield）は MCP ではなく **REST API**
  （`HIGGSFIELD_API_KEY`/`HIGGSFIELD_API_SECRET`）を直接叩いているので
  Discordボットからも動く
- **デザイン制作**（HTML→PNG）は以前 Higgsfield の MCP サンドボックス
  （`sandbox_exec`）を使っていたが、上記の理由で常に失敗していた。
  今は Mac ローカルの Playwright（`discord-groupchat/tools/html_to_png.py`、
  venv にインストール済み）でHTML→PNGを書き出す方式に変更済み。
  macOSは日本語フォント（Hiragino Sans）を標準で持っているので、
  Higgsfieldのサンドボックスと違ってフォント導入も不要になった。

**新しい機能でMCPツールを使いたくなったら、Discordボット（非対話）から
呼ぶ前に必ず「REST APIかローカル実行で代替できないか」を先に検討すること。**
MCP前提の機能はDiscordから一生動かない。

## discord-groupchat の基本

- Discord Bot 3体（オーケストレーター / Claude / Gemini）を1つの Python プロセスで動かす
- Claude 担当は `claude` CLI（サブスク・API課金なし）、Gemini は API 無料枠
- Gemini はモデルローテーション＋クールダウンで無料枠切れに対応。
  枠切れ時は Claude へフォールバック（テキスト系）またはメタ情報分析（YouTube リサーチ）
- 変更後は `python3 -m py_compile ai_group_chat.py` で構文チェックしてからコミット
